parameters:
- name: dll_path
  type: string

steps:
- task: Bash@3
  name: get_version
  displayName: 'Get version'
  inputs:
    filePath: '$(Pipeline.Workspace)/templates/azure/scripts/get_version_by_dll.sh'
  env:
    DLL_PATH: ${{ parameters.dll_path }}